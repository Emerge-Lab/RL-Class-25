[project]
name = "rl-class-25"
version = "0.1.0"
description = "Reinforcement Learning course project using PufferLib"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pufferlib>=3.0.0",
]
